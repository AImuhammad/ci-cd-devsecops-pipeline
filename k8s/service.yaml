apiVersion: v1
kind: Service
metadata:
  name: ci-cd-devsecops-app
spec:
  selector:
    app: ci-cd-devsecops-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort
